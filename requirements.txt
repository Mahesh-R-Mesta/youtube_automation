# ── Core Orchestration ────────────────────────────────────────
crewai==1.11.1

# ── LLM: Google Gemini 2.5 Flash ─────────────────────────────
# NOTE: Use google-genai, NOT google-generativeai (deprecated Nov 2025)
google-genai>=0.7.0

# ── Trend Discovery ───────────────────────────────────────────
# RSS (no key needed) + Guardian API + NewsData.io (optional free keys)
feedparser==6.0.11
# requests already listed under Visuals

# ── Text-to-Speech ────────────────────────────────────────────
# Primary TTS: free, no API key required
edge-tts==7.2.8
# Fallback TTS: free tier 10K credits/month
elevenlabs==2.40.0

# ── Visuals ───────────────────────────────────────────────────
requests==2.32.3
Pillow>=9.2

# ── Video Editing ─────────────────────────────────────────────
# NOTE: moviepy v2 has a completely different API from v1
# Use with_duration(), with_audio(), resized() — NOT v1 method names
moviepy==2.2.1

# ── YouTube Upload ────────────────────────────────────────────
google-api-python-client==2.193.0
google-auth-httplib2==0.2.0
google-auth-oauthlib==1.2.1

# ── Scheduling ────────────────────────────────────────────────
# Use APScheduler 3.x (NOT 4.x — completely different API)
APScheduler==3.11.2

# ── Config & Settings ─────────────────────────────────────────
python-dotenv>=1.1.1
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0

# ── Utilities ─────────────────────────────────────────────────
tenacity==8.5.0
numpy>=1.24,<3.0

# ── Testing ───────────────────────────────────────────────────
pytest==8.3.5
pytest-asyncio==0.24.0
